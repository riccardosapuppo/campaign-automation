# Refuse to install on a Node that cannot run this, instead of installing
# happily and failing later inside the module loader with a stack trace.
# The database is node:sqlite: there is nothing npm could install to fix it.
engine-strict=true
